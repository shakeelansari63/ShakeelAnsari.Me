Welcome, AI Agent! This file outlines the architectural patterns, guidelines, and guardrails for maintaining and extending the `shakeelansari.me` codebase. Please strictly adhere to these rules.

> **⚠️ IMPORTANT: Do NOT edit this file unless the user explicitly asks you to update AGENTS.md.**  
> If you discover a new pattern, convention, or instruction worth recording for future reference, update the relevant skill file in `skills/` instead (`skills/api/SKILL.md`, `skills/ui/SKILL.md`, or `skills/github-action/SKILL.md`).

## 🎯 Skill Selection

When starting a task, load the appropriate skill file for detailed, context-specific guidance:

| Task Type | Skill to Load |
|-----------|--------------|
| Backend API work (PHP, Slim, routes, DB) | `skills/api/SKILL.md` |
| Frontend work (React, TypeScript, styling, components) | `skills/ui/SKILL.md` |
| CI/CD / GitHub Actions / deployment | `skills/github-action/SKILL.md` |

The skill files contain exhaustive coding conventions and rules for each domain. Refer to them before writing any code.

## 🚀 Overview & Tech Stack

This project is a hybrid personal portfolio application:
*   **Frontend (`/ui`):** Single Page Application (SPA) built with **React 18**, **TypeScript**, **Vite**, **PrimeReact**, and **PrimeFlex**.
*   **Backend (`/api`):** REST API built with **PHP 8.1+**, **Slim Framework 4**, naked **PDO** for database interaction, and **JWT Auth** for administration security.
*   **Database:** **MySQL 8.0+** (Optional/Degraded mode supported).
*   **CMS Style:** Markdown-based content files (`/blogs`, `/products`, `/tutorial`) ingested and synchronized into the MySQL DB via admin endpoints.

## 📁 System Architecture Rules

### 1. The Frontend-Backend Boundary
*   **Vite Proxy:** Local dev requests to `/api/*` are automatically proxied to the PHP backend via `ui/vite.config.js`. Do not hardcode backend base URLs in React files. Use relative paths like `/api/blogs`.
*   **Degraded Mode Awareness:** The application must remain functionally stable even if the database is disconnected (`DB_HOST` empty). Ensure UI components handle empty data or specific failure states gracefully if the API layer returns structural errors or fallbacks.

### 2. Frontend Development (`/ui`)
*   **Styling:** Use **PrimeFlex** utilities for layouts and styling wherever possible instead of custom CSS. Use **PrimeReact** components for UI building blocks (Skeletons, Sidebars, Toggles).
*   **Performance Constraints:** 
    *   Keep route elements wrapped in `React.lazy()` and `Suspense` inside `App.tsx`.
    *   Do not bloat the main vendor bundle; keep third-party additions separate or lazy-loaded.
*   **Static Profile State:** Personal details (work timeline, skills) live in `ui/src/data/`. Do not create API endpoints for these; they are intentionally static.

### 3. Backend Development (`/api`)
*   **Slim 4 Routes:** All endpoints must be registered under `api/src/`. Follow the existing functional splitting pattern (e.g., Blog routing vs Admin routing).
*   **Security & Safety:**
    *   All DB connections run with a strict 30-second query execution limit (`SET SESSION max_execution_time = 30000`).
    *   PHP runtime has a `max_execution_time` cap of 60 seconds. Do not write complex long-running loops without optimizing chunk size.
    *   Admin endpoints (`/api/admin/*`) **must** gatecheck requests using the JWT verification middleware, except for the initial login endpoint.

### 4. Database Schema Changes
*   Any schema mutations must be manually appended or reflected in `api/db/schema.sql`.
*   Do not use an ORM. Use parameterized, raw SQL queries with standard PHP `PDO` statements to protect against SQL Injection.

## 🛠️ Common Workflows to Assist With

*   **Creating a new Blog feature:** Update the markdown file parsing utility in `api/src/` to sync the new metadata field, add the field to `api/db/schema.sql`, add it to the TypeScript interface model in `ui/src/models/`, and update the UI layout.
*   **Debugging local sync:** Check if the user has authenticated and that the sync hooks are hitting `/api/admin/sync-blogs` or `/api/admin/sync-learn`.

## 🚫 Critical Guardrails (What NOT to do)

1.  **Do NOT touch `[{#SEO-...#}]` strings:** The placeholders in `ui/index.html` and `api/public/sitemap.php` are heavily dependent on the CI/CD GitHub Actions pipeline. Never replace or break these placeholder formats in source files with static strings during feature refactoring.
2.  **No Node.js Backend Code:** The backend runs entirely on native PHP. Never try to inject Express, Fastify, or Node scripts for handling API functionality.
3.  **No Tailwind CSS:** The utility CSS layer is **PrimeFlex**. Do not mix utility systems or attempt to run Tailwind generation steps in Vite.
